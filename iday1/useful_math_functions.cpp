#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int x = 2;
    int y = 4;

    //int z = min(x,y);
    //int z = max(x,y);
    //int z = pow(x, y);
    //int z = sqrt(y);
    //int z = abs(-12);
    //int z = floor(2);
    int z = round(9.5);
    cout << z;
    return 0 ;
}